{
  "name": "project",
  "version": "1.0.0",
  "description": "DVA Project",
  "scripts": {
    "installf": "cd frontend && npm install",
    "installb": "cd backend && python -m venv venv && venv/bin/pip install -r requirements.txt",
    "startf": "cd frontend && npm start",
    "startb": "cd backend && venv/bin/uwsgi --socket 0.0.0.0:8000 --protocol=http -w app:app"
  },
  "license": "MIT"
}
